# Clash of Calculi Policy Pages

Static pages for Google Play setup:

- `privacy-policy.html`
- `delete-account.html`
- `index.html`

Before using these in Play Console, replace `YOUR_SUPPORT_EMAIL@example.com` in both HTML files with the real developer support email.

Enable GitHub Pages for this repository with source `main` branch and `/docs` folder.
